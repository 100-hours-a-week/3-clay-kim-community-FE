services:
  express-healthcheck-blue:
    image: seonjun1/community-front:2.0
    container_name: express-healthcheck-blue
    restart: always
    ports:
      - "3001:3000"
    networks:
      - backend-proxy

  express-healthcheck-green:
    image: seonjun1/express-healthcheck:latest
    container_name: express-healthcheck-green
    restart: always
    ports:
      - "3002:3000"
    networks:
      - backend-proxy

  nginx-proxy:
    image: nginx:stable-alpine
    container_name: nginx-proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - backend-proxy

networks:
  backend-proxy:
